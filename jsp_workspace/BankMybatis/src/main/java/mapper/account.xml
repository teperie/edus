<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0 //EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.account">
	<insert id="insertAccount" parameterType="account">
		insert into account
		(id,name,balance,type,grade)
		values(#{id},#{name},#{balance},#{type},#{grade})
	</insert>
	<select id="selectAccount" parameterType="String"
		resultType="account">
		select * from account
		where id=#{id}
	</select>
	<select id="selectAccountList" resultType="account"
		parameterType="Integer">select * from account order by id desc limit #{row}, 5</select>
	<update id="updateAccount" parameterType="account">update account set
 name=#{name}, balance=#{balance}, type=#{type}, grade=#{grade} where
 id=#{id}</update>
	<update id="updateBalance" parameterType="Map">update account set balance=#{balance} where id=#{id}</update>
	<select id="selectAccountCount" resultType="Integer">select count(*) from account</select>
</mapper>